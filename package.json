{
  "name": "keystone-ui",
  "private": true,
  "license": "MIT",
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/core",
      "packages/ui"
    ],
    "catalog": {
      "typescript": "^6"
    }
  },
  "type": "module",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "deploy": "bun run --cwd apps/web deploy",
    "check-types": "turbo check-types",
    "test": "turbo -F @keystone-ui/core -F @keystone-ui/ui -F web test",
    "test:core": "turbo -F @keystone-ui/core test",
    "test:docs": "bun --filter web test --pass-with-no-tests",
    "test:ui": "turbo -F @keystone-ui/ui test",
    "test:perf": "turbo -F @keystone-ui/core test:perf",
    "verify:release": "bun run check && bun run check-types && bun run test && bun run test:perf && bun run build",
    "dev:docs": "turbo -F web dev",
    "check": "oxlint apps packages scripts registry docs && oxfmt --write"
  },
  "devDependencies": {
    "@tanstack/solid-hotkeys": "^0.10.0",
    "@tanstack/solid-store": "^0.11.0",
    "@tanstack/solid-table": "^8.21.3",
    "oxfmt": "^0.46.0",
    "oxlint": "^1.61.0",
    "turbo": "^2.8.12",
    "typescript": "catalog:"
  },
  "packageManager": "bun@1.3.9"
}
